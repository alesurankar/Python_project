from tkinter import messagebox

# default placeholder function
def default(action):  
    messagebox.showinfo("Info", f"{action} (not implemented yet)")

# terminal_menu functions
def new_terminal(action):
    default(action)

def split_terminal(action):
    default(action)

def new_terminal_window(action):
    default(action)

def run_task(action):
    default(action)

def run_build_task(action):
    default(action)

def run_active_file(action):
    default(action)

def run_selected_text(action):
    default(action)

def show_running_tasks(action):
    default(action)

def restart_running_task(action):
    default(action)

def terminate_task(action):
    default(action)

def configure_tasks(action):
    default(action)

def configure_default_build_task(action):
    default(action)